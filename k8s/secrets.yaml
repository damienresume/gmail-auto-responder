# In production, use External Secrets Operator to pull from AWS Secrets Manager
# or GCP Secret Manager. This file is a placeholder showing the required keys.
# NEVER commit real secret values to version control.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: gmail-autoresponder
type: Opaque
stringData:
  APP_KEY: "base64:REPLACE_WITH_REAL_KEY"
  DB_USERNAME: "REPLACE"
  DB_PASSWORD: "REPLACE"
  GROQ_API_KEY: "REPLACE"
  GOOGLE_CLIENT_ID: "REPLACE"
  GOOGLE_CLIENT_SECRET: "REPLACE"
