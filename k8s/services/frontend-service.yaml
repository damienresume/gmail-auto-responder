apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: gmail-autoresponder
spec:
  selector:
    app: nextjs-frontend
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
